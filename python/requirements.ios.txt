# Hermes Mobile iOS dependencies
# Generated from upstream uv.lock core set — do not hand-edit.
# Regenerate with: scripts/gen-ios-requirements.sh
#
# Packages marked [cross-build] need the iOS wheel job.
# Packages marked [beeware] come from pypi.anaconda.org/beeware/simple.
# Packages marked [omit] are excluded on iOS (see ADR-002 D3).

# ── Pure Python (from PyPI) ────────────────────────────────────────────────
openai>=1.82.0,<2
fastapi>=0.115.12,<1
starlette>=0.46.2,<1
uvicorn>=0.41.0,<1
httpx>=0.28.1,<1
httpcore>=1.1.0,<2
h11>=0.16.0,<1
anyio>=4.9.0,<5
sniffio>=1.3.1,<2
certifi>=2025.4.26
idna>=3.10,<4
requests>=2.32.3,<3
urllib3>=2.4.0,<3
rich>=14.0.0,<15
prompt_toolkit>=3.0.52,<4
wcwidth>=0.2.13,<1
jinja2>=3.1.6,<4
Markdown>=3.8.2,<4
pydantic>=2.12.0,<3
typing_extensions>=4.13.2,<5
annotated_types>=0.7.0,<1
PyJWT>=2.10.1,<3
croniter>=6.0.7,<7
python-dateutil>=2.9.0,<3
six>=1.17.0,<2
toml>=0.10.2,<1
click>=8.1.8,<9
python-dotenv>=1.0.1,<2
python-multipart>=0.0.20,<1
humanize>=4.12.3,<5
aiofiles>=24.1.0,<25
websockets>=15.0.1,<16
pygments>=2.19.1,<3
platformdirs>=4.3.6,<5
filelock>=3.18.0,<4
tiktoken>=0.9.0,<1
regex>=2024.11.6
distro>=1.9.0,<2
tqdm>=4.67.1,<5

# ── Native with official iOS wheels (from PyPI) ───────────────────────────
pillow>=12.3.0,<13

# ── Native with BeeWare iOS wheels [beeware] ──────────────────────────────
cffi>=1.17.1,<2
ruamel.yaml.clib>=0.2.12,<1

# ── Native with pure-Python fallbacks (install --no-binary) ───────────────
pyyaml>=6.0.2,<7
markupsafe>=3.0.2,<4
charset-normalizer>=3.4.2,<4

# ── Cross-build required [cross-build] ────────────────────────────────────
pydantic-core>=2.46.4,<3
jiter>=0.16.0,<1

# ── Omitted on iOS [omit] ─────────────────────────────────────────────────
# cryptography — only used by Bitwarden secrets + Weixin adapter, neither runs on iOS
# uvloop — uvicorn uses asyncio loop
# httptools — uvicorn uses h11
# watchfiles — not needed
# nemo-relay — platform marker excludes iOS
# firecrawl-anydoc — no iOS wheel, lazy-optional
# ptyprocess — guarded by pty_bridge
# psutil — stubbed by hermes_mobile_boot.py
